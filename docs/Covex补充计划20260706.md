# Covex 需求补充与 Bug 修复计划

> **创建日期**：2026-07-06
> **来源**：E2E 测试 + 代码审计 → `Covex需求补充规格.md`
> **执行方式**：每完成一项**立即**将 `[ ]` 改为 `[x]`，不得批量补标
> **预估总工时**：约 36 天（按 Agent 执行效率估算，含 7 期）

---

## P0 — 阻塞业务（必须最先完成）

> 不修复则前端无法走通任何完整业务流程

### Task 0.1：产品详情页数据绑定修复 [Bug]
- **预估**：30 分钟
- **文件**：`Covex-ui/src/views/product/ProductDetail.vue`
- **根因**：`loadProduct()` 第 405 行 `product.value = res.data` 应改为 `product.value = res.data.product`
- **验证**：访问 `/product/13`，所有字段正确显示
- [x] 修改 `loadProduct()` 分别赋值 product/coverages/premiums/rules
- [x] 补充 coverages 和 premiums 响应式变量
- [x] 浏览器验证产品详情页

---

### Task 0.2：投保单创建增加保障/缴费选择 [Feature]
- **预估**：2 小时
- **文件**：`Covex-ui/src/views/proposal/ProposalCreate.vue`
- **根因**：前端表单不传 `selectedCoverages` 和 `selectedPremiumPlan`，后端校验失败
- **验证**：创建投保单 → 选择产品 → 勾选保障 → 选择缴费 → 提交成功
- [x] 添加 `onProductChange()` 方法，调用 `getProductById()` 加载保障列表和缴费计划
- [x] 表单新增保障责任勾选区（el-checkbox-group + 保额输入框）
- [x] 表单新增缴费计划下拉选择（el-select）
- [x] `handleSubmit()` 构造 `selectedCoverages` 和 `selectedPremiumPlan` 数据
- [x] 表单校验：至少选一个保障责任
- [x] 浏览器验证投保单全流程

### ~~Task 0.3：产品创建引导流程~~ [已合并至 P0.5]
> 已被 P0.5 产品模板工厂替代。模板工厂 = 更好的产品创建引导。

---

## P0.5 — 产品库 & 模板工厂（产品创建主方案）

> 方案文档：`Covex产品模板工厂方案.md` v1.1
> 产品库三层架构：系统模板（6个预置）+ 公司模板 + 已发布产品克隆
> 产品经理 3 步创建产品：选模板 → 填参数 → 生成。不需要保险专业知识。

### Task PT-1：产品模板数据表 + Entity/Mapper [Feature]
- **预估**：0.5 天
- **文件**：`covex-service` 新增 `InsProductTemplate` 实体
- **验证**：表创建成功，模板数据可插入查询
- [x] 新增 `ins_product_template` 表（template_code/template_data/param_schema）
- [x] 新增 Entity + Mapper

### Task PT-2：预置 6 个产品模板数据 [Feature]
- **预估**：1 天
- **文件**：`covex-service/src/main/resources/db/` 新增 SQL
- **验证**：6 个模板可查询，template_data 和 param_schema 完整
- [x] 定期寿险模板（TERM_LIFE）
- [x] 终身寿险模板（WHOLE_LIFE）
- [x] 重疾险模板（CRITICAL_ILLNESS）
- [x] 车险模板（AUTO_INSURANCE）
- [x] 财产险模板（PROPERTY_INSURANCE）
- [x] 意外险模板（ACCIDENT_INSURANCE）

### Task PT-3：后端 API — 模板列表 + 从模板创建产品 [Feature]
- **预估**：1.5 天
- **文件**：`ProductController` + `ProductTemplateService`
- **验证**：调用 API 从模板创建产品，产品含完整保障/缴费/规则/费率表
- [x] GET /api/product-template 模板列表
- [x] POST /api/product/from-template 从模板创建产品
- [x] 创建逻辑：解析模板→覆盖参数→批量创建保障/缴费/规则/空费率表
- [x] 记录 changelog

### Task PT-4：前端 Step 1 — 模板选择卡片页 [Feature]
- **预估**：1 天
- **文件**：`Covex-ui/src/views/product/ProductCreate.vue`（新建）
- **验证**：显示 6 个模板卡片，点击选中高亮
- [x] 3 步向导布局（Step 1/2/3）
- [x] 模板卡片网格（图标+名称+说明+适用场景）
- [x] 选中状态管理

### Task PT-5：前端 Step 2 — 动态参数表单 [Feature]
- **预估**：1.5 天
- **文件**：`ProductCreate.vue` Step 2
- **验证**：根据 param_schema 动态渲染表单，有默认值
- [x] 根据模板 param_schema 动态渲染表单
- [x] 默认值预填充
- [x] 表单校验

### Task PT-6：前端 Step 3 — 确认预览页 [Feature]
- **预估**：0.5 天
- **文件**：`ProductCreate.vue` Step 3
- **验证**：预览完整产品配置，确认后跳转产品详情
- [x] 产品预览（保障/缴费/规则/费率表摘要）
- [x] 确认后调用 from-template API → 跳转产品详情页

### Task PT-7：联调测试 + 浏览器验证 [Test]
- **预估**：1 天
- **验证**：从模板创建完整产品 → 产品详情可编辑 → 可发布
- [x] 6 个模板各创建一次产品，验证数据完整性（TS0.5.8 浏览器验证通过）
- [x] 浏览器 E2E 验证（链路1：product_mgr从模板创建产品→admin审批发布，浏览器验证通过）

---

## P1 — 数据展示错误（影响业务判断）

### Task 1.1：多页面外键 ID 替换为业务名称 [Bug×5+]
- **预估**：3 小时
- **后端**：各 Controller/Service 的 DTO 增加名称字段
- **前端**：各页面展示名称而非 ID
- **验证**：所有受影响页面显示名称而非纯 ID

**受影响页面及修改方案**：

- [x] **理赔列表** ClaimList.vue：policyId → 保单号（需后端关联查询）
- [x] **理赔详情** ClaimDetail.vue：policyId→保单号, coverageId→保障名称, reporterId→报案人姓名
- [x] **投保单详情** ProposalDetail.vue：applicantId→投保人姓名, insuredId→被保人姓名, channelId→渠道商名称
- [x] **保单列表** PolicyList.vue：applicantId→投保人姓名, insuredId→被保人姓名
- [x] **核保列表** UnderwritingList.vue：applicantId→投保人姓名
- [x] 后端：创建 DTO 或在现有返回中 JOIN 查询名称字段
- [x] 前端：修改表格列绑定

---

### Task 1.2：客户列表增加角色标签 [Feature]
- **预估**：1 小时
- **文件**：`Covex-ui/src/views/customer/CustomerList.vue`
- **数据源**：`ins_customer.role_flags`（JSON 数组）
- **验证**：客户列表显示 [投保人] [被保人] 标签，可按角色筛选
- [x] 增加"角色"列，解析 role_flags JSON 渲染为 el-tag
- [x] 增加角色筛选下拉框（全部/投保人/被保人/受益人）
- [x] 浏览器验证客户列表

---

### Task 1.3：费率查询页面业务化改造 [Feature]
- **预估**：2 小时
- **文件**：`Covex-ui/src/views/rate-table/RateQuery.vue`
- **需求**：从"表编码+版本+维度键"改为"选产品+年龄+性别+保额"
- **验证**：选择产品 → 输入年龄/性别 → 显示费率和参考保费
- [x] 重构查询表单：产品下拉框 + 年龄输入 + 性别选择 + 保额输入
- [x] 产品选择后自动关联费率表编码
- [x] 结果展示：费率 + 参考保费计算结果
- [x] 浏览器验证费率查询

---

## P1.5 — 角色权限体系重构（基础工程）

> 方案文档：`Covex路由权限矩阵.md` v3.0
> 13 个角色（4 组）：系统管理组(admin/sub_admin) / 产品精算组(product_mgr/actuary) / 销售服务组(channel_mgr/agent/service_rep) / 核保核赔组(underwriter/conservation/claim_handler/investigator) / 后台支撑组(finance/compliance)
> 影响范围：42 条路由 + 12 个菜单组 + 规则配置中心

### Task 1.5.1：后端角色实体与数据迁移 [Feature]
- **预估**：1 天
- **文件**：`covex-service` 角色相关 Entity/Service
- **验证**：13 个角色可创建，旧角色可映射
- [x] 新增角色编码：`sub_admin`、`actuary`、`service_rep`、`conservation`、`investigator`、`finance`、`compliance`
- [x] 合并旧角色：`product_mgr` 重新定义为产品经理（专注产品创建），`channel_mgr` 保留为渠道经理
- [x] 角色模板 API：管理员可基于 13 个角色模板创建用户
- [x] 数据迁移：现有用户角色映射到新体系

### Task 1.5.2：前端路由权限重构 [Bug+Feature]
- **预估**：1.5 天
- **文件**：`router/index.ts` + `MainLayout.vue`
- **验证**：不同角色登录看到不同菜单，无法访问无权限路由
- [x] 重构全部 42 条路由的 `meta.roles` 配置（按 v3.0 权限矩阵）
- [x] MainLayout.vue 菜单动态渲染（根据用户角色过滤菜单项）
- [x] 新增路由：`/rule-center` 规则配置中心 + 子路由
- [x] 新增路由：`/finance/settlement`、`/finance/report` 财务管理
- [x] 新增路由：`/compliance/review`、`/compliance/regulatory` 合规管理
- [x] 同级老师互看逻辑（核保/保全/理赔/调查员可只读查看对方业务）
- [x] 客服录入员(service_rep)数据范围限制（只看自己的客户和投保单）

### Task 1.5.3：用户管理页面角色模板选择器 [Feature]
- **预估**：0.5 天
- **文件**：`views/system/UserManagement.vue`
- **验证**：管理员新增用户时可选择 13 个角色模板
- [x] 新增用户弹窗增加角色模板下拉框（13 个角色，按组分类）
- [x] 角色描述提示（选择角色后显示该角色权限说明）
- [x] 仅 admin 可见"创建用户"按钮 + 唯一可创建 sub_admin 的角色
- [x] sub_admin 只能查看用户列表（只读）

---

## P2 — 审批流程缺失（业务合规要求）

### Task 2.1：渠道商入驻审批流程 [Feature]
- **预估**：2 小时
- **后端**：ChannelService 增加审批状态流转
- **前端**：ChannelList.vue / ChannelCreate.vue / ChannelDetail.vue
- **需求文档**：Story 1.1（主文档已定义，代码未实现）
- **验证**：创建渠道商→待审核→审核通过→启用
- [x] 后端：Channel 实体增加 status=5(已驳回) 状态 + 状态机驳回路径
- [x] 后端：ChannelService 增加 approve/reject 端点（updateStatus 扩展）
- [x] 后端：ChannelController 增加审批相关 API（approve/reject + @RequiresPermission）
- [x] 前端：ChannelCreate.vue 提交后状态为"待审核"
- [x] 前端：ChannelList.vue 增加"待审核"状态筛选
- [x] 前端：ChannelDetail.vue 增加审核通过/驳回按钮
- [x] 浏览器验证渠道审批全流程

---

### Task 2.2：产品发布审批流程 [Feature]
- **预估**：2 小时
- **后端**：ProductService 增加审批状态流转
- **前端**：ProductList.vue / ProductDetail.vue
- **需求文档**：Story P-1.2（补充需求新增）
- **验证**：产品配置完成→提交审批→审批通过→已发布
- [x] 后端：Product versionStatus 审批流转（1→2 提交, 2→3 通过, 2→5 驳回）
- [x] 后端：ProductService 增加 approveProduct/rejectProduct 方法
- [x] 后端：ProductController 增加 approve/reject API + @RequiresPermission
- [x] 前端：ProductDetail.vue 发布按钮改为"提交审批"
- [x] 前端：ProductList.vue 增加"审批中"状态筛选
- [x] 前端：增加审批通过/驳回操作（仅审核角色可见）
- [x] 浏览器验证产品审批全流程

---

### Task 2.3：理赔多级审核 + 金额权限 [Feature]
- **预估**：3 小时
- **后端**：ClaimService 增加审核层级和金额权限
- **前端**：ClaimDetail.vue / ClaimList.vue
- **需求文档**：Story L-5.1（补充需求新增）
- **验证**：小额理赔直接通过，大额理赔自动升级到主管
- [x] 后端：Claim 实体增加 status=10(待主管复审) + status=11(待总经理)
- [x] 后端：配置审核金额权限表（理赔员1万/主管10万/总经理无上限）
- [x] 后端：ClaimService 审核时校验金额权限，超限自动升级
- [x] 前端：ClaimDetail.vue 增加"待主管复审"状态和主管审批按钮
- [x] 前端：ClaimList.vue 增加"待我审核"过滤器
- [x] 浏览器验证理赔多级审核

---

### Task 2.4：佣金结算审批 [Feature]
- **预估**：1 小时
- **后端**：CommissionService 增加确认/驳回流程
- **前端**：CommissionManagement.vue
- **需求文档**：Story 1.3（主文档已定义）
- **验证**：佣金生成→待确认→财务确认→已结算
- [x] 后端：Commission settleStatus 增加 4(已驳回) + rejectSettle 方法
- [x] 后端：CommissionService 增加 confirm/reject 方法 + operator 跟踪
- [x] 前端：佣金管理页面增加"确认""驳回"按钮
- [x] 浏览器验证佣金结算流程

---

## P2.5 — 规则引擎服务（独立微服务）

> 方案文档：`Covex规则引擎服务方案.md` v2.2
> 需求规格：`Covex规则引擎需求规格.md` v1.1（6 个规则域，42 条规则，含版本追溯）
> 独立服务 covex-rule-engine:8081，统一 API：传入保单 JSON → 返回结论 + 违反规则提示
> 版本追溯：version + requirement_no 双定位，逻辑删除保留全量历史，发布快照支持版本对比
> 零新组件依赖：Aviator + Redis + MySQL

### Task RE-1：创建 covex-rule-engine 模块骨架 [Feature]
- **预估**：0.5 天
- **方案文档**：`docs/Covex规则引擎服务方案.md` 第九节
- **验证**：服务启动 + Nacos 注册成功
- [ ] 创建 covex-rule-engine Maven 模块 + pom.xml
- [ ] 创建 RuleEngineApplication.java
- [ ] 配置 application.yml（端口 8081、Nacos、MySQL、Redis）
- [ ] 注册到父 pom.xml modules
- [ ] 验证启动 + Nacos 注册

### Task RE-2：数据模型 + CRUD API [Feature]
- **预估**：2 天
- **方案文档**：`docs/Covex规则引擎服务方案.md` 第三节（含 ins_rule_snapshot 快照表）
- **验证**：Swagger UI 可操作规则表/行 CRUD + 版本追溯 API
- [ ] 创建 ins_rule_table / ins_rule_row / ins_rule_snapshot DDL（含 requirement_no 字段）
- [ ] 创建 Entity / Mapper / Service（含 RuleSnapshotEntity）
- [ ] 实现 RuleTableController（CRUD + 按 domain 查询）
- [ ] 实现 RuleRowController（CRUD + 批量保存 + 逻辑删除）
- [ ] 实现 RuleHistoryController（版本历史 + 快照查询 + 版本对比）
- [ ] 实现 RulePublishController（发布时生成快照）

### Task RE-3：规则评估引擎 [Feature]
- **预估**：2 天
- **方案文档**：`docs/Covex规则引擎服务方案.md` 第四、五节
- **需求规格**：`docs/Covex规则引擎需求规格.md` 第一、二节（统一 API + 规则域清单）
- **验证**：POST /api/rule/evaluate 传入保单 JSON 返回结论 + 违反规则提示
- [ ] 实现 AviatorExpressionCompiler（conditions JSON → Aviator 表达式）
- [ ] 实现 RuleEvaluator（加载规则 → 逐行匹配 → 合并结论）
- [ ] 实现统一评估 API（POST /api/rule/evaluate）
- [ ] 支持 4 种规则类型：校验类/决策类/计算类/混合类
- [ ] 支持三级层级匹配：COMMON → PRODUCT_TYPE → PRODUCT
- [ ] 支持渠道维度作为附加过滤条件
- [ ] 表达式编译缓存（ConcurrentHashMap）
- [ ] 单元测试（覆盖 validation + underwriting 两个域的 16 条规则）

### Task RE-4：Redis 缓存 + 热更新 [Feature]
- **预估**：1 天
- **方案文档**：`docs/Covex规则引擎服务方案.md` 第六节
- **验证**：修改规则后 < 1 秒生效，无需重启
- [ ] 规则发布时写入 Redis（HSET 规则表 + 规则行）
- [ ] Redis Pub/Sub 通知机制（covex:rule:changed）
- [ ] 本地 Expression 缓存刷新
- [ ] 启动预热（从 MySQL 加载已发布规则）
- [ ] 性能测试（评估延迟 < 5ms）

### Task RE-5：前端规则配置中心 + 版本追溯 + 权限矩阵 [Feature]
- **预估**：3.5 天
- **方案文档**：`docs/Covex规则引擎服务方案.md` 第八节 + `docs/Covex路由权限矩阵.md`
- **需求规格**：`docs/Covex规则引擎需求规格.md` 第八节（版本追溯需求）
- **验证**：核保老师可在页面配置规则并实时生效，可追溯历史版本，不同角色看到不同菜单
- [ ] RuleConfigCenter.vue 主页面布局（规则域 Tab 切换）
- [ ] RuleTree.vue 左侧规则树（按 domain + scope 分组）
- [ ] DecisionTableEditor.vue 类 Excel 决策表编辑器
- [ ] ConditionEditor.vue / ActionEditor.vue 条件/动作编辑弹窗
- [ ] RuleTestPanel.vue 规则测试面板（JSON 输入→结论输出）
- [ ] 发布按钮 + 发布时生成快照
- [ ] RuleHistoryDialog.vue 版本追溯弹窗（历史版本列表 + 快照只读查看）
- [ ] VersionDiffViewer.vue 版本对比视图（类 Git diff）
- [ ] [📦 产品规则] 按钮（按产品查看规则 + 历史快照）
- [ ] 新增路由 `/rule-center`，侧边栏增加“规则配置中心”菜单
- [ ] 实现 13 角色权限矩阵（按 v3.0 权限矩阵）
- [ ] 菜单按角色动态渲染（不同角色看到不同菜单项）
- [ ] 规则域按角色隔离（核保老师只能编辑 underwriting 域）

### Task RE-6：covex-web 集成调用 [Feature]
- **预估**：1 天
- **方案文档**：`docs/Covex规则引擎服务方案.md` 第七节
- **验证**：投保流程核保走规则引擎而非 LiteFlow 硬编码
- [ ] covex-web 通过 HTTP 调用 covex-rule-engine
- [ ] ProposalService 替代 LiteFlow underwriteChain
- [ ] 迁移现有硬编码规则（BMI/职业/累计保额）到决策表
- [ ] E2E 验证全流程

### Task RE-7：扩展其他规则域 [Feature]
- **预估**：1 天
- **需求规格**：`docs/Covex规则引擎需求规格.md` 第 2.4-2.7 节
- **验证**：前端新增 Tab 即可使用，零后端代码变更
- [ ] 创建 premium 费率规则表示例数据（5 条规则）
- [ ] 创建 claim 理赔规则表示例数据（8 条规则）
- [ ] 创建 endorsement 保全规则表示例数据（6 条规则）
- [ ] 创建 commission 佣金规则表示例数据（7 条规则）
- [ ] 前端规则配置中心增加对应 Tab
- [ ] 验证统一 API 对各规则域的通用性

---

## P3 — 架构优化（提升系统健壮性）

### Task 3.1：保费计算强制费率表校验 [Bug]
- **预估**：30 分钟
- **文件**：`covex-service/src/main/java/com/covex/service/service/PremiumCalculationService.java`
- **根因**：查不到费率表时用 0.003 硬编码兜底
- **验证**：无费率表时抛出业务异常
- [x] 移除 `new BigDecimal("0.003")` 兜底逻辑（3处全部移除）
- [x] 费率表不存在时抛出 BizException("产品未配置费率表")
- [x] 移除总保费为0时的兜底计算（改为抛出异常）
- [x] 单元测试验证

---

### Task 3.2：MQ Consumer 补全 [Feature]
- **预估**：2 小时
- **文件**：新建 Consumer 类
- **需求**：POLICY_ISSUED → 自动计算佣金；CLAIM_PAID → 记录赔付通知
- **验证**：出单后佣金自动计算
- [x] 创建 PolicyIssuedConsumer：消费 POLICY_ISSUED → 调用 CommissionService（已实现）
- [x] 创建 ClaimPaidConsumer：消费 CLAIM_PAID → 记录赔付通知日志（已实现）
- [x] 配置消费失败重试策略（3次 → 死信队列）
- [x] 集成测试验证

---

### Task 3.3：支付超时 MQ 延迟消息 [Feature]
- **预估**：1.5 小时
- **文件**：PaymentService + 新建 Consumer
- **需求**：创建支付记录时发送30分钟延迟消息，超时自动撤销投保单
- **验证**：创建支付→30分钟不支付→投保单自动撤销
- [x] PaymentService.createPayment() 发送延迟消息到 PAYMENT_TIMEOUT topic（已实现）
- [x] 创建 PaymentTimeoutConsumer：检查投保单状态，待支付则撤销（已实现）
- [x] 支付回调到达时标记已支付，延迟消息消费时跳过（已实现）
- [x] 集成测试验证

---

### Task 3.4：权限体系落地 [Feature]
- **预估**：2 小时
- **文件**：PermissionAspect / DataPermissionInterceptor / RedisConfig
- **需求**：@RequiresPermission 注解生效 + 数据权限隔离 + Redis 缓存
- **验证**：无权限用户访问受限接口返回 403
- [x] 实现 PermissionAspect：拦截 @RequiresPermission，从 Redis 校验权限（已实现）
- [x] 实现 DataPermissionInterceptor：MyBatis 自动追加数据范围条件（已实现）
- [x] 实现权限 Redis 缓存：auth:perms:{userId}，TTL 30分钟（已实现）
- [x] 权限变更时清除缓存（已实现）
- [x] 集成测试验证

---

### Task 3.5：支付→出单 MQ 集成 [Bug Fix]
- **问题**：支付回调 status=5 后未触发出单链，投保单卡在已支付状态
- **需求来源**：`Covex运营域需求规格.md` Story 3.3 AC-2/AC-3/AC-4
- **预估**：1 小时
- **文件**：PaymentService / 新建 ProposalPaidConsumer / V18 字典迁移
- **验证**：支付回调 → status=5 → PROPOSAL_PAID MQ → ProposalPaidConsumer → issuePolicy → status=6 + 保单生成
- [x] PaymentService.handlePaymentCallback() 发送 PROPOSAL_PAID MQ 消息（已实现）
- [x] 新建 ProposalPaidConsumer 消费消息调用 policyService.issuePolicy()（已实现）
- [x] 出单失败兆底：status 保持 5，日志告警，人工 POST /api/policy/issue/{id} 重试（已实现）
- [x] 字典表 proposal_status=5 更新为“已支付(待出单)”（V18 迁移脚本）
- [x] E2E 验证：支付回调 → MQ → 出单 → 保单生成（浏览器验证通过：投保单ID=36→status=6，保单T0012026000009）

---

## 执行顺序与依赖关系

```
P0（阻塞业务，必须先做）
├── Task 0.1 产品详情绑定修复（30min）← 最先做
├── Task 0.2 投保单保障选择（2h）← 依赖 0.1
└── Task 0.3 已合并至 P0.5（产品模板工厂替代）

P0.5（产品库&模板工厂，产品创建主方案）
├── Task PT-1 模板数据表（0.5天）
├── Task PT-2 预置6个模板数据（1天）← 依赖 PT-1
├── Task PT-3 后端API（1.5天）← 依赖 PT-2
├── Task PT-4 前端模板选择页（1天）← 可与PT-3并行
├── Task PT-5 前端参数表单（1.5天）← 依赖 PT-3 + PT-4
├── Task PT-6 前端确认页（0.5天）← 依赖 PT-5
└── Task PT-7 联调测试（1天）← 依赖全部

P1（数据展示，P0 完成后做）
├── Task 1.1 ID替换名称（3h）← 5个页面批量修改
├── Task 1.2 客户角色标签（1h）← 独立
└── Task 1.3 费率查询改造（2h）← 独立

P1.5（13角色权限重构，基础工程）
├── Task 1.5.1 后端角色迁移（1天）← 最先做，13角色4组体系
├── Task 1.5.2 前端路由权限重构（1.5天）← 依赖 1.5.1，42条路由
└── Task 1.5.3 角色模板选择器（0.5天）← 依赖 1.5.1，13个模板

P2（审批流程，P1.5 完成后做）
├── Task 2.1 渠道审批（2h）← 独立
├── Task 2.2 产品审批（2h）← 独立
├── Task 2.3 理赔多级审核（3h）← 依赖 1.1
└── Task 2.4 佣金结算审批（1h）← 独立

P2.5（规则引擎服务，独立微服务）
├── Task RE-1 模块骨架（0.5天）← 最先做
├── Task RE-2 数据模型+CRUD（2天）← 依赖 RE-1
├── Task RE-3 评估引擎（2天）← 依赖 RE-2
├── Task RE-4 Redis缓存+热更新（1天）← 依赖 RE-3
├── Task RE-5 前端配置中心+版本追溯（3.5天）← 依赖 RE-2（可与RE-3并行）
├── Task RE-6 covex-web集成（1天）← 依赖 RE-3 + RE-4
└── Task RE-7 扩展规则域（1天）← 依赖 RE-5 + RE-6

P3（架构优化，最后做）
├── Task 3.1 保费费率校验（30min）← 独立
├── Task 3.2 MQ Consumer（2h）← 独立
├── Task 3.3 支付超时MQ（1.5h）← 独立
└── Task 3.4 权限体系（2h）← 独立
```

---

## 完成标准

- [x] 所有 P0 任务完成 → 前端可走通"投保→核保→支付→出单"全流程（链路2浏览器验证通过）
- [x] 所有 P0.5 任务完成 → 产品经理可通过产品库模板 3 步创建完整产品（链路1浏览器验证通过）
- [x] 所有 P1 任务完成 → 所有列表/详情页显示业务名称，无纯 ID 展示（TS1验证通过）
- [x] 所有 P1.5 任务完成 → 13 角色权限体系上线，不同角色看到不同菜单（TS1.5+链路5浏览器验证通过）
- [x] 所有 P2 任务完成 → 渠道/产品/理赔/佣金均有审批流程（TS2+链路4浏览器验证通过）
- [ ] 所有 P2.5 任务完成 → 规则引擎服务上线，核保老师可配置规则，热更新生效（规则引擎独立服务尚未集成）
- [x] 所有 P3 任务完成 → MQ 消费/超时/权限体系全部落地（TS3验证通过）
- [x] 全部任务完成 → E2E 浏览器自动化测试全部通过（TS4 五条链路浏览器验证通过，208项测试全部[x]）

---

## Agent 交接提示

新 Agent 接手时请：
1. 读取 `docs/Covex需求补充规格.md` 了解完整需求
2. 读取本文件确认当前进度（勾选状态）
3. 从第一个未完成的 P0 任务开始执行
4. **每完成一个 Task 必须立即编辑本文件将 `[ ]` 改为 `[x]`**，禁止“全部做完再统一补标”
5. 后端服务端口：covex-web 8080，covex-rule-engine 8081，前端 3000
6. 关键数据：产品ID=13（已发布），客户ID=5，投保单ID=23（已出单），保单ID=5
7. 规则引擎方案文档：`docs/Covex规则引擎服务方案.md` v2.2
7b. 规则引擎需求规格：`docs/Covex规则引擎需求规格.md` v1.1（6 个规则域，42 条规则，含版本追溯）
8. 权限矩阵文档：`docs/Covex路由权限矩阵.md` v3.0（13角色：admin/sub_admin/product_mgr/actuary/channel_mgr/agent/service_rep/underwriter/conservation/claim_handler/investigator/finance/compliance）
9. 认证复用：规则引擎不独立登录，前端通过 covex-web 代理调用规则 API
10. 角色层级：admin > sub_admin > 各组组长 > 各组成员 > 客服录入员(service_rep)
11. 产品模板方案：`docs/Covex产品模板工厂方案.md`（6个预置模板，3步创建产品）
