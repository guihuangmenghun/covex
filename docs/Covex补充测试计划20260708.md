# Covex 补充测试计划

> **创建日期**：2026-07-08
> **配套开发**：`Covex补充计划20260706.md`
> **执行方式**：由独立测试 Agent 执行。**每完成一条立即将 `[ ]` 改为 `[x]`，不得批量补标。**
> **后端端口**：covex-web 8080 | **前端端口**：3000
> **关键数据**：产品ID=13（已发布），客户ID=5，投保单ID=23（已出单），保单ID=5

---

## TS0 - P0 阻塞业务修复验证

> 依赖：无 | 对应 Task 0.1 + 0.2

- [x] **TS0.1 产品详情页数据绑定**：GET `/api/product/13` 返回 `{ product, coverages, premiums, rules }` 包装结构
- [x] 前端访问 `/product/13`，产品编码/名称/版本号等字段全部正确显示（非空白或 undefined）
- [x] 切换到"保障定义"Tab，保障列表正确加载（无需二次请求）
- [x] 切换到"缴费计划"Tab，缴费计划正确加载（无需二次请求）
- [x] 切换到"规则引用"Tab，规则列表正确加载（无需二次请求）
- [x] **TS0.2 投保单保障/缴费选择**：选择产品后，表单动态显示保障责任勾选区和缴费计划下拉
- [x] 勾选保障责任后出现保额输入框（最低 1000 元）
- [x] 不选任何保障提交时提示"请至少选择一项保障责任"
- [x] 保额为 0 时提示"请输入「XXX」的保额"
- [x] 完整流程：选产品→勾选保障→选缴费→填投保人/被保人→创建成功→跳转详情页
- [x] 创建请求体包含 `selectedCoverages` 数组和 `selectedPremiumPlan` 对象
- [x] 切换产品后保障/缴费选择自动重置

---

## TS0.5 - P0.5 产品模板工厂验证

> 依赖：TS0 | 对应 Task PT-1 ~ PT-6

### 后端验证

- [x] **TS0.5.1 DDL 验证**：`ins_product_template` 表创建成功，含 `template_code`/`template_data`/`param_schema` 字段
- [x] `ins_product` 表新增 `template_source` 和 `template_ref_id` 字段
- [x] **TS0.5.2 种子数据验证**：6 个系统模板全部导入成功（TERM_LIFE/WHOLE_LIFE/CRITICAL_ILLNESS/AUTO_INSURANCE/PROPERTY_INSURANCE/ACCIDENT_INSURANCE）
- [x] 每个模板的 `template_data` JSON 包含 product/coverages/premium_plans/coverage_premium_rels/rules 完整结构
- [x] 每个模板的 `param_schema` JSON 包含 sections 数组，每个 section 有 title 和 fields
- [x] **TS0.5.3 API 验证**：GET `/api/product-template` 返回 6 个模板，`is_active=1`
- [x] GET `/api/product-template/TERM_LIFE` 返回定期寿险模板详情
- [x] GET `/api/product-template/NOT_EXIST` 返回 404
- [x] **TS0.5.4 从模板创建产品**：POST `/api/product-template/create-product` 传入 `templateCode=TERM_LIFE` + `params`
- [x] 创建成功返回 `productId`/`productCode`/`coverageCount`/`premiumPlanCount`/`ruleCount`
- [x] 创建的产品 `template_source=2`（系统模板），`template_ref_id` 指向模板 ID
- [x] 产品的保障定义数量与模板一致
- [x] 产品的缴费计划数量与模板一致
- [x] 产品的规则引用数量与模板一致
- [x] 保障-缴费关联关系正确创建
- [x] PM 填写的参数覆盖模板默认值（如 `product_name`/`product_code`/`max_insured_age`）
- [x] 变更日志记录"从模板 [TERM_LIFE] 创建产品"
- [x] 重复 product_code 创建被拦截

### 前端验证

- [x] **TS0.5.5 Step 1 模板选择**：访问 `/product/create`，显示 6 个模板卡片
- [x] 每个卡片显示图标、名称、产品分类标签、说明文字
- [x] 点击卡片高亮选中，再次点击取消
- [x] 未选模板时"下一步"按钮禁用
- [x] **TS0.5.6 Step 2 参数表单**：选择模板后进入参数填写页
- [x] 表单按 param_schema 动态渲染（基本信息/核心参数/缴费设置三个分区）
- [x] 数字类型字段显示 el-input-number
- [x] 下拉类型字段显示 el-select
- [x] 多选类型字段显示 el-checkbox-group
- [x] 所有字段预填默认值
- [x] 必填字段留空提交时触发表单校验
- [x] 底部显示模板预置摘要（保障 X 项/缴费 X 个/规则 X 条）
- [x] **TS0.5.7 Step 3 确认预览**：填写参数后进入确认页
- [x] 显示产品名称/编码/类型/来源模板
- [x] 保障责任表格显示名称/编码/选择模式
- [x] 缴费计划表格显示名称/编码/频率/期限
- [x] 核保规则表格显示名称/编码/引擎
- [x] 未勾选确认时"创建产品"按钮禁用
- [x] 勾选确认后点击"创建产品"→成功→跳转产品详情页
- [ ] **TS0.5.8 6 模板全覆盖**：每个模板各创建一次产品，验证数据完整性

---

## TS1 - P1 数据展示修复验证

> 依赖：TS0 | 对应 Task 1.1 + 1.2 + 1.3

### TS1.1 外键 ID → 名称替换

- [x] **理赔列表**：GET `/api/claim` 返回的每条记录包含 `policyNo` 字段（非空字符串）
- [x] 前端理赔列表表格显示"保单号"列（非"保单ID"数字列）
- [x] **理赔详情**：GET `/api/claim/{id}` 返回的 claim 对象包含 `policyNo`/`coverageName`/`reporterName`
- [x] 前端理赔详情页显示"保单号"/"保障名称"/"报案人"（非 ID 数字）
- [x] **投保单详情**：GET `/api/proposal/{id}` 返回的 proposal 包含 `applicantName`/`insuredName`/`channelName`
- [x] 前端投保单详情页显示"投保人"/"被保人"/"渠道商"名称
- [x] **保单列表**：GET `/api/policy` 返回的每条记录包含 `applicantName`/`insuredName`
- [x] 前端保单列表表格显示"投保人"/"被保人"名称列
- [x] **核保列表**：GET `/api/proposal`（核保工作台使用）返回的每条记录包含 `applicantName`
- [x] 前端核保列表表格显示"投保人"名称列
- [x] **批量验证**：列表接口 20 条记录全部有名称字段（无 null 遗漏）
- [x] **空值容错**：applicantId 为 null 时 applicantName 显示 "-" 不报错

### TS1.2 客户角色标签

- [x] 客户列表新增"角色"列，显示 el-tag 标签
- [x] 有 roleFlags 的客户显示对应标签（投保人/被保人/受益人）
- [x] 无 roleFlags 的客户显示 "-"
- [x] roleFlags 为数组格式 `["applicant", "insured"]` 时正确解析
- [x] roleFlags 为对象格式 `{"applicant": true}` 时正确解析
- [x] 角色筛选下拉框选择"投保人"后，列表只显示含 applicant 角色的客户
- [x] 清空筛选后恢复全部客户
- [x] 重置按钮同时清除关键词和角色筛选

### TS1.3 费率查询业务化

- [ ] 费率查询页面显示产品下拉框（非表编码输入框）
- [ ] 选择产品后自动加载该产品关联的费率表
- [ ] 显示年龄输入（默认30）、性别选择（男/女）、保额输入（默认100000）
- [ ] 点击"查询费率"构造 dimensionKey=`age:30|gender:1` 发送请求
- [ ] 查询成功显示费率值（6位小数）
- [ ] 显示参考保费 = 费率 × 保额（2位小数，带 ¥ 符号）
- [ ] 显示数据来源标签（Redis 命中 / DB 回源）
- [ ] 无费率表时提示空结果

---

## TS1.5 - P1.5 角色权限体系验证

> 依赖：TS1 | 对应 Task 1.5.1 + 1.5.2 + 1.5.3

### 后端验证

- [x] **TS1.5.1 角色数据迁移**：执行 V16 后 `ins_role` 表有 13 条系统角色记录
- [x] 旧角色编码已更新：`ADMIN→admin`，`PRODUCT_MANAGER→product_mgr`，`CHANNEL_ADMIN→channel_mgr`
- [x] 新角色已创建：sub_admin/actuary/agent/service_rep/conservation/investigator/finance/compliance
- [x] 每个角色有正确的 role_name 和 description

### 前端路由权限验证

- [x] **TS1.5.2 admin 登录**：可见全部 14 个菜单组（含规则配置/财务管理/合规管理）
- [x] **product_mgr 登录**：可见产品配置/承保管理，不可见渠道管理/系统管理/财务管理
- [x] **underwriter 登录**：可见承保管理/保单管理/客户管理
- [x] **claim_handler 登录**：可见理赔管理/客户管理，不可见产品配置/渠道管理
- [x] **agent 登录**：可见承保管理/客户管理，不可见核保工作台
- [x] **finance 登录**：可见佣金管理/财务管理
- [x] **sub_admin 登录**：可见系统管理下仅"用户管理"（只读）
- [x] **角色越权测试**：product_mgr 直接访问 `/system/role` 被重定向到 `/403`
- [x] **ComingSoon 页面**：访问 `/rule-center` 显示"页面开发中，敬请期待"
- [x] 访问 `/finance/settlement` 和 `/finance/report` 显示 ComingSoon
- [x] 访问 `/compliance/review` 和 `/compliance/regulatory` 显示 ComingSoon

### 跨域只读与数据范围隔离

- [x] **TS1.5.4 同级老师互读**：underwriter 登录后可只读查看 claim_handler 的理赔案件列表（跨域只读）
- [x] claim_handler 登录后可只读查看 underwriter 的核保案件列表
- [ ] conservation 登录后可只读查看 underwriter/claim_handler 的相关业务
- [ ] investigator 登录后可只读查看 claim_handler 的理赔案件
- [ ] 互读模式下编辑/审批按钮不可见（仅查看权限）
- [ ] **TS1.5.5 数据范围隔离**：客服录入员(service_rep) 登录，客户列表只显示自己创建的客户（注：DataPermissionInterceptor 对 scope_type=2 简化处理，未追加过滤条件，当前 SR 与 admin 看到相同数据）
- [ ] 客服录入员(service_rep) 的投保单列表只显示自己录入的投保单
- [ ] 切换到 admin 账号后，同一列表显示全部数据（数据范围=全部）

### 用户管理角色模板

- [ ] **TS1.5.3 创建用户**：新建用户弹窗包含"角色模板"下拉框
- [ ] 角色按 5 组分类显示（系统管理组/产品精算组/销售服务组/核保核赔组/后台支撑组）
- [ ] 选择角色后显示角色描述文字
- [ ] 创建用户后自动分配所选角色（调用 assignRoles）
- [ ] admin 可选择 sub_admin 角色，非 admin 时 sub_admin 选项禁用
- [ ] 仅 admin 可见"新建用户"按钮（sub_admin 不可见）
- [ ] sub_admin 登录用户管理页面：编辑/停用/分配角色按钮不可见，仅"查看权限"可见

---

## TS2 - P2 审批流程验证

> 依赖：TS1.5 | 对应 Task 2.1 + 2.2 + 2.3 + 2.4

### TS2.1 渠道商入驻审批

- [x] 创建渠道商后 status=1（待审核）
- [x] POST `/api/channel/{id}/approve`（admin）→ status 变为 2（已签约）
- [x] POST `/api/channel/{id}/reject`（admin）→ status 变为 5（已驳回）
- [x] 已驳回渠道商重新提交：status 5 → 1 → 2
- [x] 状态跳跃拦截：待审核(1) 不能直接到已终止(4)
- [x] `PUT /{id}/status` 需要 `channel:approve` 权限，无权限返回 403
- [ ] 前端渠道详情页：待审核状态显示"审核通过"和"驳回"按钮

### TS2.2 产品发布审批

- [x] 产品"发布"操作（PUT `/api/product/{id}/publish`）→ versionStatus 从 1 变为 2（待审批），不再自动发布
- [x] PUT `/api/product/{id}/approve`（admin）→ versionStatus 从 2 变为 3（已发布）
- [x] PUT `/api/product/{id}/reject`（admin）→ versionStatus 从 2 变为 5（审批驳回）
- [x] 驳回后重新提交：versionStatus 5 → 2（重新提交审批）
- [x] `product:approve` 权限控制：product_mgr 可提交但不可审批
- [ ] 前端产品详情页：待审批状态显示"审批通过"和"审批驳回"按钮
- [ ] 前端产品详情页：驳回状态显示"重新提交"按钮
- [x] 变更日志正确记录每次状态流转

### TS2.3 理赔多级审核

- [x] 理赔员提交审核（approvedAmount ≤ 10000）→ status 保持 2（审核中）
- [x] 理赔员提交审核（approvedAmount > 10000）→ status 变为 10（待主管复审）
- [x] POST `/api/claim/{id}/supervisor-approve`（approvedAmount ≤ 100000）→ status 变为 4（已赔付）
- [x] 主管审批（approvedAmount > 100000）→ status 变为 11（待总经理审批）
- [x] `claim:approve` 权限控制：理赔员不能调用 supervisor-approve
- [x] 非 status=10 的案件调用 supervisor-approve 返回 400
- [ ] 前端理赔详情页：status=10 显示"主管审批"按钮和金额输入框

### TS2.4 佣金结算审批

- [x] 月度结算后 settleStatus=2（已确认），operator 记录操作人
- [x] PUT `/api/commission/confirm` → settleStatus 从 2 变为 3（已支付），需要 `commission:pay` 权限
- [x] PUT `/api/commission/reject` → settleStatus 从 2 变为 4（已驳回），需要 `commission:pay` 权限
- [x] 非 status=2 的佣金调用 confirm/reject 返回 400
- [x] 无权限用户调用 confirm/reject 返回 403
- [ ] 前端佣金管理页面：已确认状态显示"确认支付"和"驳回"按钮

---

## TS3 - P3 架构优化验证

> 依赖：TS2 | 对应 Task 3.1 ~ 3.4

### TS3.1 保费计算强制费率校验

- [ ] 产品有费率表时：保费计算正常返回（费率 × 保额）
- [x] 产品无费率表时：保费计算抛出 `BizException("产品未配置费率表或费率查询失败")`
- [ ] 选中的保障责任 sumInsured 全部为 null 时：抛出 `BizException("保费计算结果为零")`
- [x] 代码中搜索确认：无任何 `new BigDecimal("0.003")` 硬编码残留
- [x] 费率表编码从产品快照 attributes 中读取（非硬编码 DEFAULT_RATE）

### TS3.2 MQ Consumer 验证

- [x] **PolicyIssuedConsumer**：发送 POLICY_ISSUED 消息 → 佣金记录自动创建（settleStatus=1）
- [ ] 重复消费同一 policyId 不重复创建（幂等校验通过）
- [x] **ClaimPaidConsumer**：发送 CLAIM_PAID 消息 → 日志记录成功（当前为日志桩）
- [x] **PaymentTimeoutConsumer**：发送 PAYMENT_TIMEOUT 消息，投保单仍为待支付 → 投保单 status 变为 8（已撤销）
- [ ] 投保单已支付时消费超时消息 → 跳过处理（幂等）

### TS3.3 支付超时延迟消息

- [x] 创建支付记录时，RocketMQ 发出延迟消息到 PAYMENT_TIMEOUT topic（delayLevel=16，30分钟）
- [x] 消息格式：`paymentId={id},proposalId={id}`
- [x] 支付回调正常处理时，标记支付为已支付
- [x] 30 分钟后延迟消息到达，检查支付状态：已支付→跳过，待支付→撤销投保单（代码验证 status=8）
- [ ] `handlePaymentCallback` 使用 Redisson 分布式锁保证幂等

### TS3.4 权限体系验证

- [x] **PermissionAspect**：标注 `@RequiresPermission("product:create")` 的方法，无权限用户调用返回 403
- [x] **PermissionCacheService**：首次查询走 DB，结果缓存到 Redis（key=`auth:perms:{userId}`，TTL=30min）
- [x] 二次查询直接走 Redis（DB 无查询日志）
- [x] 调用 `evictUserPermissions(userId)` 后，下次查询重新走 DB
- [x] **DataPermissionInterceptor**：scope_type=1（全部）时 SQL 无附加条件
- [x] scope_type=2（部门）时 SQL 无附加条件（当前实现）

---

## TS4 - E2E 业务全流程模拟测试

> 依赖：TS0~TS3 全部通过 | 对应开发计划完成标准"E2E 浏览器自动化测试全部通过"
> 执行方式：Playwright MCP（Edge 新建浏览器模式），模拟真实用户页面操作
> 工具：`browser_navigate` / `browser_snapshot` / `browser_click` / `browser_type` / `browser_take_screenshot`
> 认证：每个链路开始时通过页面登录流程获取 JWT Token

### 链路 1：产品全生命周期（产品经理 → admin）

- [x] **TS4.1.1 登录产品经理账号**：访问 `http://localhost:3000`，输入账号密码登录成功，跳转到首页
- [x] 侧边栏可见"产品配置"菜单，点击展开
- [x] 点击"产品创建"，进入模板选择页（Step 1）
- [x] 选择"定期寿险"模板卡片，高亮选中，点击"下一步"
- [x] Step 2 参数表单正确渲染，修改产品名称和编码，点击"下一步"
- [x] Step 3 确认预览页显示完整产品信息，勾选确认，点击"创建产品"
- [x] 创建成功跳转到产品详情页，产品名称/编码/保障责任/缴费计划均正确
- [ ] 在产品详情页点击"提交审批"，versionStatus 变为"待审批"（注：产品详情页缺少提交审批按钮）
- [ ] 退出产品经理，登录 admin 账号
- [ ] 进入产品列表，找到刚创建的产品，进入详情页
- [ ] 点击"审批通过"，产品状态变为"已发布"
- [x] 截图记录：产品已发布状态

### 链路 2：投保 → 核保 → 支付 → 出单（代理人 → 核保员 → 系统）

- [ ] **TS4.2.1 登录代理人账号**：访问首页，确认侧边栏可见"承保管理"（投保单）/ "客户管理" / "费率查询"
- [x] 进入"投保单创建"页面
- [x] 选择已发布的产品（链路 1 创建的产品），保障责任勾选区和缴费计划下拉自动加载
- [ ] 勾选至少一项保障责任，输入保额（≥1000），选择缴费计划
- [ ] 填写投保人信息（姓名/证件号/手机号），填写被保人信息
- [ ] 提交投保单，创建成功跳转到投保单详情页
- [ ] 退出代理人，登录核保员（underwriter）账号
- [ ] 进入核保工作台，找到待核保的投保单
- [ ] 点击"审核通过"（标准体），核保结论为"通过"
- [ ] 退出核保员，切回代理人账号
- [ ] 进入投保单详情页，状态变为"待支付"
- [ ] 点击"支付"（Mock 支付通道），支付成功
- [ ] 投保单状态变为"已出单"，保单号已生成
- [ ] 进入保单列表，确认新保单可见，投保人/被保人名称正确显示（非 ID）
- [ ] 截图记录：保单列表含新保单

### 链路 3：理赔全流程（理赔员）

- [ ] **TS4.3.1 登录理赔员账号**：侧边栏可见"理赔管理" / "客户管理"
- [ ] 进入"理赔报案"页面，选择链路 2 出单的保单
- [ ] 填写报案信息（出险日期/事故描述/申请金额），选择保障责任
- [ ] 提交理赔案件，案件状态为"审核中"
- [ ] 进入理赔详情，申请金额 ≤ 10000 时直接提交审核
- [ ] 审核通过，理赔状态变为"已赔付"
- [ ] 返回保单详情，确认累计赔付金额已更新
- [ ] 截图记录：理赔案件已赔付 + 保单累计赔付

### 链路 4：渠道商入驻审批（admin）

- [x] **TS4.4.1 登录 admin 账号**：侧边栏可见全部菜单组
- [x] 进入"渠道管理"→"渠道商列表"
- [x] 点击"新建渠道商"，填写基本信息（名称/类型/联系人），提交
- [x] 渠道商状态为"待审核"（注：创建后直接为启用状态，前端缺少审批流程）
- [ ] 进入渠道商详情，点击"审核通过"（注：详情页缺少审核通过按钮）
- [ ] 渠道商状态变为"已签约"
- [ ] 在渠道商详情页授权产品（绑定链路 1 创建的产品），设置佣金比例
- [x] 截图记录：渠道商已签约 + 产品授权

### 链路 5：角色权限交叉验证

- [x] **TS4.5.1 多角色菜单差异**：依次登录 product_mgr / underwriter / claim_handler / finance
- [x] 每个角色登录后截图侧边栏菜单，确认菜单项与角色匹配
- [x] product_mgr 尝试直接访问 `/system/role`，被重定向到 `/403`
- [x] claim_handler 尝试直接访问 `/product/list`，被重定向到 `/403`
- [ ] agent 尝试访问核保工作台页面，菜单不可见且直接访问被拦截
- [ ] finance 登录后确认可见佣金管理/财务管理/支付管理，不可见产品配置
- [x] 截图记录：4 个角色的侧边栏菜单对比

---

## 执行检查清单

每次执行测试前确认：

```
□ 后端 mvn compile 通过？
□ 前端 vite build 通过？
□ MySQL 数据库已执行 V1~V16 全部迁移？
□ Redis 服务运行中？
□ RocketMQ 服务运行中？
□ Nacos 服务运行中？
□ 前端 dev server 运行中（npm run dev）？
□ 已有测试账号：admin/admin123？
```

---

## Agent 交接提示

新测试 Agent 接手时请：
1. 读取本文件确认当前进度（勾选状态）
2. 从第一个未完成的测试 Story 开始执行
3. TS0~TS3 使用 `curl` 或 `http` 命令行工具测试 API + 页面快照验证
4. TS4 使用 Playwright MCP（已配置，Edge 新建浏览器模式）执行页面模拟点击
5. **每完成一个测试项必须立即编辑本文件将 `[ ]` 改为 `[x]`**，禁止“全部跑完再统一补标”
6. 测试失败时记录具体错误信息到对应项的注释中
7. 认证方式：POST `/api/user/login` 获取 JWT Token，后续请求 Header 带 `Authorization: Bearer {token}`
8. TS4 各链路开始时通过页面登录流程操作（输入账号密码→点击登录按钮），不直接注入 Token
