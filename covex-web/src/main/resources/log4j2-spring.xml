<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="LOG_PATH">./logs</Property>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-}] %-5level %logger{36} - %msg%n</Property>
        <Property name="JSON_PATTERN">{"timestamp":"%d{yyyy-MM-dd'T'HH:mm:ss.SSS'Z'}","level":"%level","thread":"%thread","traceId":"%X{traceId:-}","logger":"%logger","message":"%msg","exception":"%ex"}%n</Property>
    </Properties>

    <Appenders>
        <!-- Console (human-readable) -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- Application log (JSON for ELK) -->
        <RollingFile name="AppFile"
                     fileName="${LOG_PATH}/covex.log"
                     filePattern="${LOG_PATH}/covex-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${JSON_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="100MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>

        <!-- Audit log (key business events) -->
        <RollingFile name="AuditFile"
                     fileName="${LOG_PATH}/audit.log"
                     filePattern="${LOG_PATH}/audit-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${JSON_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="100MB"/>
            </Policies>
            <DefaultRolloverStrategy max="90"/>
        </RollingFile>

        <!-- Error log -->
        <RollingFile name="ErrorFile"
                     fileName="${LOG_PATH}/error.log"
                     filePattern="${LOG_PATH}/error-%d{yyyy-MM-dd}-%i.log.gz">
            <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${JSON_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="50MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Audit logger for key business events -->
        <Logger name="audit" level="INFO" additivity="false">
            <AppenderRef ref="AuditFile"/>
            <AppenderRef ref="Console"/>
        </Logger>

        <!-- Application logger -->
        <Logger name="com.covex" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AppFile"/>
            <AppenderRef ref="ErrorFile"/>
        </Logger>

        <!-- Framework loggers -->
        <Logger name="org.springframework" level="INFO"/>
        <Logger name="com.baomidou" level="INFO"/>
        <Logger name="com.yomahub.liteflow" level="INFO"/>
        <Logger name="org.apache.rocketmq" level="WARN"/>
        <Logger name="org.redisson" level="WARN"/>
        <Logger name="com.alibaba.nacos" level="WARN"/>

        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AppFile"/>
            <AppenderRef ref="ErrorFile"/>
        </Root>
    </Loggers>
</Configuration>
